[package]
name = "cubecode-contracts"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Shared runtime contracts (events, routes) for cubecode layers."

[dependencies]
